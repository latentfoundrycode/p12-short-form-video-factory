# Frontend

React with TypeScript, built by Vite.

- `npm run dev` — dev server
- `npm run build` — build into `app/web/`

Never edit `app/web/` directly — it is build output.

HyperFrames compositions are plain HTML generated by Python elsewhere in the repo and are not React components.
