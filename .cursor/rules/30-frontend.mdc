---
globs: frontend/**/*.ts, frontend/**/*.tsx, frontend/**/*.css
---

# Frontend

React with TypeScript. Vite builds into `app/web/`.

HyperFrames compositions are plain HTML generated by Python. They are never React components.
